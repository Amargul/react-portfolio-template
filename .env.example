# =============================================================================
# Environment variables — OPTIONAL, UNUSED BY DEFAULT
# =============================================================================
#
# You do NOT need a .env file to run this project.
# All site content (name, contact, social links, meta) is configured in:
#   src/config/site.config.js  ← single source of truth
#
# This file exists only for future extensibility. If you later add features
# that require environment-specific or secret values (e.g. a contact form
# API key, analytics ID), you can:
#   1. Add variables here as commented examples (e.g. # VITE_CONTACT_FORM_KEY=)
#   2. Copy this file to .env and set real values locally or in your host
#   3. Never commit .env or real secrets
#
# Safe to commit this file as-is.
# =============================================================================
